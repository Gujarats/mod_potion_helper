# Debug Logging

Potion Helper exposes a `Debug Logging` setting on its MSU General page. It defaults to enabled for development.

When enabled, diagnostic messages are written to:

`C:\Users\gujar\Documents\Battle Brothers\log.html`

Disable the setting to suppress Potion Helper debug output without changing gameplay behavior. Re-enable it before investigating marketplace, potion-use, or compatibility issues.
